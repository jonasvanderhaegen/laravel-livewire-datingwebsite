<button
    type="button"
    id="{{ $tab }}-tab"
    data-tabs-target="#{{ $tab }}"
    role="tab"
    aria-controls="{{ $tab }}"
    aria-selected="false"
    class="rounded-full px-5 py-1.5 text-xs font-medium text-gray-900 dark:text-white"
>
    {{ $tab }}
</button>
